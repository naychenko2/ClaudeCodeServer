# Финальное ревью ветки `feature/notes-optional`

Дата: 2026-09-10. Ревьюер: Глеб (код-ревью), финальный проход по ветке.
Предмет: пилот «отключаемая подсистема» на вертикали Notes — тумблер
`Subsystems:Notes:Enabled`, вынос контроллера в `ClaudeHomeServer.Notes`,
nullable-зависимости на спине, гейт входа и вкрапления на фронте, страница
подсистем в админке.

База сравнения — `de4d7490` (точка ветвления), HEAD — `b5212bb0`.
`master` влит в ветку дважды (`a56159ad`, `088e5f65`) и сам ушёл вперёд,
поэтому `de4d7490..HEAD` содержит чужую работу (этап 5, волны Spend/Tasks/
волна C, инверсия контрибьюторов) — в цене пилота она не учитывается.

## Вердикт одной строкой

**Мержить нельзя — один блокер.** Гейт не изолирует REST-слой вертикали:
при `Subsystems:Notes:Enabled=false` маршруты `/api/notes/*` отдают **500**,
а не 404, потому что явное подключение `ApplicationPart` — мёртвый код.
Всё остальное, ради чего затевался пилот, сделано чисто: правило
nullable-зависимостей выполнено без исключений (23 точки), гейт ровно один,
деградация осмысленная, контракт фронт/бэк закрыт сторожем, который
действительно краснеет.

Предыдущие блокеры (Б1 — десять обязательных ctor-зависимостей, Б2 —
незакрытый форвардер `IKnowledgeSyncParticipant`, Б3 — ни одного теста на
гейт, Ф1 — условный хук в `NotesWidget`) закрыты все четыре.

## Проверки, которые прогнаны

| Проверка | Результат |
|---|---|
| `dotnet build` (всё решение, worktree `notes-optional`) | **зелёная, 0 ошибок** |
| `dotnet test --filter "NotesDisabled\|NotesSubsystemGate\|PromptSectionContributorsDi\|SubsystemBoundary\|IlBoundaryRegression\|RootSubsystemBoundary"` | **69 пройдено, 4 упало** (все 4 — блокер Б1 ниже) |
| `npx tsc -b` | зелёный |
| `npm run lint:design` | зелёный |
| `npx vitest run src/lib/subsystems.test.ts src/lib/__tests__/subsystems.contract.test.ts` | **24 пройдено** |
| Мутационная проверка контракта `subsystems` (4 мутации) | **4 из 4 краснеют** — см. раздел ниже |
| Эксперимент «отключить `AddApplicationPart` целиком» | **результат не изменился** — доказательство мёртвого кода |

Связка, которую важно прочесть правильно: прошлый проход имел 6950 зелёных
тестов при неработающей фиче. Теперь тест уровня хоста **есть**, и он фичу
ловит. Это главное улучшение ветки по сравнению с волной 1 — появился
источник правды, который красит красным, а не успокаивает.

---

## Находки

### Blocker

#### Б1. `/api/notes/*` отдаёт 500 вместо 404 при выключенной подсистеме

Задача в трекере: `4defaacc-7220-4f9f-8472-ef6ae37ec5f8`.

При `Subsystems:Notes:Enabled=false` MVC **находит** `NotesController` и
пытается его активировать. Зависимостей (`NotesService`,
`NotesKnowledgeService`, `NotesAiService`, `NoteTaskSyncService`) в
контейнере нет — `NotesSubsystem.Register` не вызывался, — активация падает,
`UnhandledExceptionHandler` (`Services/Http/UnhandledExceptionHandler.cs:55`)
ставит `Status500InternalServerError` и пишет `LogError` со стектрейсом.
На каждый запрос.

404 vs 500 здесь не косметика: 500 означает, что отключение подсистемы её от
приложения не изолировало. Для решения по кольцу 2 это центральный факт.

**Свидетельство.** 4 кейса из 13 в `NotesDisabledTests` красные:

```
Не пройден МаршрутыЗаметок_ПриВыключеннойПодсистеме_404АНе500(method: "GET",  url: "/api/notes")
Не пройден МаршрутыЗаметок_ПриВыключеннойПодсистеме_404АНе500(method: "GET",  url: "/api/notes/sources")
Не пройден МаршрутыЗаметок_ПриВыключеннойПодсистеме_404АНе500(method: "GET",  url: "/api/notes/caps")
Не пройден МаршрутыЗаметок_ПриВыключеннойПодсистеме_404АНе500(method: "POST", url: "/api/notes")
  Expected response.StatusCode to be HttpStatusCode.NotFound {value: 404} …,
  but found HttpStatusCode.InternalServerError {value: 500}.
```

**Корень — доказан экспериментом, а не чтением.** Явное подключение части
(`ClaudeHomeServer.Notes/Services/Notes/NotesSubsystem.cs:90-96`,
`IConfigureOptions<MvcOptions>` → `partManager.ApplicationParts.Add(new
AssemblyPart(assembly))`) не делает ничего: сборка `ClaudeHomeServer.Notes`
попадает в `ApplicationPartManager` сама — она собрана под
`Microsoft.NET.Sdk.Web` и является `ProjectReference` Main, поэтому штатное
автообнаружение частей по зависимостям entry-сборки внутри `AddControllers()`
добавляет её независимо от гейта.

Проверено так: условие заменено на `if (false && SubsystemGate.IsEnabled(config,
Key))`, то есть часть не добавляется НИКОГДА; прогон `NotesDisabledTests` дал
**ровно тот же счёт 4/9**, включая зелёный контрольный кейс
`МаршрутыЗаметок_ПриВключённойПодсистеме_НеТеряютсяРоутером`. Если бы явное
добавление что-то решало, контрольный кейс упал бы. Мутация откачена,
сборка после отката зелёная.

Следствие для документации: комментарии `NotesSubsystem.cs:44-47`,
`NotesController.cs:13-14` и шапка в `Program.cs` утверждают «маршруты
`/api/notes/*` отдают 404, а не 500». Это неправда — править вместе с кодом.

### Major

#### М1. `POST /api/briefing/today` отдаёт 500 при выключенной подсистеме

Задача: `3bb00d85-6e46-4464-86c4-c733359e2df0`.

`DailyBriefingService.BuildAndWriteAsync:135-136` при `_notes is null` бросает
`InvalidOperationException`. Путь по расписанию этот бросок глотает
(`MaybeRunScheduledAsync`, `catch` → `LogWarning`) — там всё правильно,
и проверка стоит ПЕРВОЙ строкой, до дорогого `BuildBriefAsync`.
Но REST-путь не защищён ничем: `BriefingController.Today:21-26` зовёт
`GenerateAsync` без `try/catch`, фильтра на этот тип исключения нет, и до
клиента доезжает 500 от `UnhandledExceptionHandler`.

Постановка волны 1 требовала «без исключений и без 500» — здесь требование
не выполнено. Лечение очевидное и однородное с соседом: своё исключение вместо
`InvalidOperationException` плюс `catch` в контроллере — ровно как
`SummaryGenerationException` → 502 у саммари сессии.

#### М2. Тест, который ловит Б1, лежит в worktree вне git

Задача: `7bc928e7-c9f6-4243-80e2-d15e05ce36b3`.

`backend/ClaudeHomeServer.Tests/Subsystems/NotesDisabledTests.cs` (17 886 байт,
13 кейсов) — единственная запись `git status` на HEAD:

```
?? backend/ClaudeHomeServer.Tests/Subsystems/
```

Правило CLAUDE.md про общий worktree написано именно про это: untracked-файлы
не защищены ни `git reflog`, ни `fsck`, и окно «готово, но не закоммичено»
равно окну необратимой потери (инцидент 2026-09-01). Любой `git clean` уносит
13 кейсов и доказательство блокера Б1 вместе с ними.

Сам тест написан хорошо и заслуживает отдельной строки в отчёте: он не только
проверяет, но и **доказывает, что проверяет** — у каждого «пусто/404» рядом
стоит контрольный кейс на включённой подсистеме. Плюс в шапке разобрана
главная грабля задачи: `Subsystems:Notes:Enabled=false` доезжает до
`AddSubsystems` через `IWebHostBuilder.UseSetting` (хост-конфигурация →
аргументы командной строки → `CreateBuilder(args)`), а не через env процесса,
которая протекала бы в соседние конкурентные хосты.

Коммитить красным, ассерт не ослаблять: красное здесь — свойство продукта,
не теста.

### Minor

#### м1. `SessionSummaryService` платит за LLM до проверки гейта

Задача: `a096558c-421c-45e1-b4ea-f1c05bf888f1`.

В `SummarizeAsync` дорогой `cheap.RunAsync` стоит на строках 57-68, а проверка
`notes is null` — на 71-72. Отказ корректный (502, не 500), но при выключенной
подсистеме пользователь платит за конспект, который гарантированно будет
выброшен: сохранять его некуда, и это известно до первого токена. Рядом, в
`DailyBriefingService`, тот же гейт стоит первой строкой — сделать однородно.
Комментарий строк 26-27 сейчас объясняет поздний порядок как задуманный.

#### м2. Гейт внутри `Register` тождественно истинен

`NotesSubsystem.cs:90` — `if (SubsystemGate.IsEnabled(config, Key))` внутри
метода, который `AddSubsystems` вызывает только при пройденном гейте.
Комментарий строк 86-89 это честно признаёт («технически `Register()` и так
вызывается только при пройденном гейте»), объясняя правку как снятие второго
инлайн-читателя конфига — и как снятие дубля она верна. Но условие от этого
не перестало быть всегда-`true`. Вместе с Б1 блок мёртв дважды: и условие
лишнее, и тело ничего не меняет. Уйдёт при починке Б1.

#### м3. Трейлер `CCS-Session` — у 7 коммитов из 17

Есть: `d8c73a9e`, `7dfc34dd`, `ac3fa27d`, `a4b7b992`, `7952419b`, `6a9f6159`,
`b5212bb0`.
Нет: `b8f5af19`, `827c1f75`, `e83f6082`, `e1d759e4`, `c83d0647`, `aad7b60e`,
`6dc01caf`, `f1212ef2`, `d67889f4`, `b46b7624`.

Трейлер — единственный способ связать коммит с сессией, которая его породила;
на половине ветки эта связь потеряна.

#### м4. Авторство и домен соавтора

- `b8f5af19` подписан другой личностью: `naych <naych@local>` вместо
  `Найченко Андрей <andrey@naychenko.ru>` у остальных шестнадцати.
- `Co-Authored-By: MiniMax-M3 <noreply@anthropic.com>` (`b8f5af19`) и
  `Co-Authored-By: qwen3.8-27b <noreply@anthropic.com>` (`d8c73a9e`) — домен
  вендора неверный. Правило CLAUDE.md: домен берётся по вендору модели.
  В `b5212bb0` та же модель подписана правильно — `<noreply@minimax.io>`.
- `ac3fa27d`, `a4b7b992`, `7952419b` — без `Co-Authored-By` вовсе.

### Что сделано хорошо (не формальность, а то, на что стоит опираться в кольце 2)

1. **Структурный гейт вместо процедурного.** Контрибьютор секции промпта
   больше не гейтится ни предикатом, ни пост-хок удалением дескриптора: он
   регистрируется внутри `NotesSubsystem.Register`, а выключенная подсистема
   `Register` не вызывает. Гейт получился свойством графа DI, а не кодом,
   который можно забыть позвать. Это лучшая форма из трёх опробованных.
2. **Тест, доказывающий сам себя.** Паттерн «проверка + контрольный кейс на
   включённой подсистеме рядом» в `NotesDisabledTests` снимает целый класс
   ложнозелёных: «404 пришёл от опечатки в маршруте», «ноль инструментов —
   от ошибки в `tools/list`», «нет секции — от опечатки в ключе».
3. **Сторож контракта, который краснеет.** `subsystems.contract.test.ts`
   проверен мутацией с обеих сторон и по обоим уровням — см. ниже.

---

## Вердикт по правилу nullable-зависимостей

**Правило выполнено полностью.** Обязательных параметров конструктора от
отключаемой вертикали вне самой вертикали не осталось ни одного — проверены
все четыре типа, которые регистрирует `NotesSubsystem.Register`.

| Тип | Опциональных точек на спине | Обязательных вне вертикали |
|---|---:|---:|
| `NotesService` | 13 | **0** |
| `NotesKnowledgeService` | 6 | **0** |
| `NoteTaskSyncService` | 3 | **0** |
| `NotesAiService` | 1 | **0** |
| Итого | **23** | **0** |

Обязательные параметры остались только ВНУТРИ `ClaudeHomeServer.Notes`
(`NotesController`, `NoteExpiryService`, `NotesAiService`,
`NotesKnowledgeService`, `NoteTaskSyncService`, `NotesRecallContributor`) — это
легально и единственно правильно: при выключенном гейте ни один из них не
регистрируется, резолвить их некому.

Форс-разыменований нет: грепом по `_notes!`, `notes!.`, `_kb!`, `kb!.`,
`_notesKb!`, `noteSync!` — ни одного вхождения на спине. Все 23 точки читаются
через `?.`, `?? []`, `is null` → ранний выход или `is not { } local` →
narrowing.

Места, приехавшие с мержами master, проверены отдельно: `PersonaBindingsService`
(правлен в `088e5f65`) и `TaskExecutionService` держат опциональную форму,
`NotesRecallContributor` уехал в вертикаль целиком (`eb787230`).

## Вердикт по единственности точки гейта

**Одна.** `SubsystemGate.IsEnabled`
(`ClaudeHomeServer.Core/Services/Composition/IAppSubsystem.cs:116-124`) —
единственное место, читающее `Subsystems:{key}:Enabled`. Вызывающих четыре:
`SubsystemRegistration.AddSubsystems:177`, `SubsystemStateStore.Snapshot:87`,
`SubsystemHostingExtensions:55`, `NotesSubsystem.Register:90` (последний —
тождественно истинный, см. м2).

Инлайновых `config.GetValue<bool>("Subsystems:…")` нет нигде: грепом по всему
`backend/` остальные `GetValue` относятся к `InspectionMode`,
`Testing:EnableHostedServices` и `Reader:AcceptLanguage`. После мержей master
второй реализации не появилось.

## Вердикт по исключениям в брифинге и сводке сессии

Постановка требовала «без исключений и без 500». Вердикт разный для двух мест.

| Место | Что при `notes == null` | Вердикт |
|---|---|---|
| `SessionSummaryService.SummarizeAsync` | `SummaryGenerationException` → `SessionSummaryController` ловит → **502** с текстом | **Оправданный случай.** Операция по смыслу пишет заметку; без vault писать некуда, пустой успех был бы ложью. Клиент получает понятный отказ, лог чистый. Оговорка — м1: отказ выносится после платного вызова модели. |
| `DailyBriefingService.BuildAndWriteAsync` | `InvalidOperationException`; по расписанию → `LogWarning`, по REST → **500** | **Дефект (М1).** Путь по расписанию в порядке, REST-путь нарушает требование прямо: необработанное исключение, ProblemDetails 500, `LogError` со стектрейсом. |

То есть «бросок исключения при `notes == null`» сам по себе дефектом не
является — дефект в том, что у одного из двух мест нет обработчика на границе.

Остальные точки деградации осмысленны:

- `NotesToolset` — `ToolsFor` отдаёт пустой список, `CallAsync` —
  `Deny("Подсистема Notes отключена")`. Модель не видит инструментов, которые
  не отработают (подтверждено кейсом `СоставИнструментовЗаметок_…_Пуст`).
- `NoteTriggerSource` — пустой список событий.
- `UnifiedSearchService`, `PersonasController`, `PersonasCrudService`,
  `PersonaBindingsService` — пустая выдача (`?? []`), поиск и карточки персон
  работают без раздела заметок.
- `TaskExecutionService`, `ProjectsController`, `TasksController`,
  `ChatDigestService`, `DocsController`, `FilesController`, `WorkspaceToolset` —
  no-op на необязательном побочном действии (`notesKb?.QueueSync`,
  `RewriteAnnotationTargets` под `if`).
- `PersonaMemoryService` — `memory_to_note`/`memory_from_note` возвращают
  `null`/`false`, то есть честный «не смог», а не тихий успех.

Кейс `СоседниеВертикали_ПриВыключенныхЗаметках_ОтвечаютШтатно` подтверждает
следствие на уровне хоста: `GET /api/tasks` и `GET /api/projects` отдают 200.

## Вердикт по гейту контрибьюторов промпта (находка Дениса)

Постановка описывает состояние на `a4b7b992`: гейт перенесён с пост-хок
удаления дескрипторов на предикат при регистрации. **На HEAD ветки форма
третья и лучшая** — после мержа `7f1eafe4` (инверсия контрибьюторов) гейт
стал структурным, и предикат тоже не нужен.

- **Пост-хок удаления нет.** Греп по `RemoveAll`/`Remove(…Descriptor`/
  `ServiceDescriptor` в `backend/` (без тестов): единственное удаление
  дескрипторов — `Program.cs:968`, снятие `IHostedService` в `InspectionMode`,
  к контрибьюторам отношения не имеет. Мёртвого кода от прошлой формы не
  осталось.
- **Предиката тоже нет, и он не нужен.** `AddPromptSectionContributor<T>`
  (`Core/Services/Turn/PromptSectionContributorRegistrationExtensions.cs`)
  параметра-предиката не имеет — мёртвого параметра не завелось.
  `PromptSectionContributorsDi.AddPromptSectionContributors` держит только
  пять «чистых» контрибьюторов Turn, без заметок.
- **Покрытие полное, потому что регистрация одна.**
  `NotesRecallContributor` регистрируется ровно в одном месте —
  `NotesSubsystem.cs:69`. Второго пути в `IEnumerable<IPromptSectionContributor>`
  нет; греп по всему `backend/` даёт одно вхождение с `Add`.
- **Подтверждено на живом хосте.** Кейс
  `КонтрибьюторыПромптаХоста_ПриВыключеннойПодсистеме_БезСекцииЗаметок`
  зелёный и проверяет три вещи сразу: `recall-notes` нет при выключенной,
  есть при включённой, и набор отличается **ровно на одну секцию** — чужие
  вертикали гейт заметок не проредил.

## Мутационная проверка сторожа контракта `subsystems`

Требование постановки — проверить мутацией, а не чтением. Базовый прогон:
`subsystems.contract.test.ts` — 3 зелёных, `subsystems.test.ts` — 21 зелёный.

| № | Мутация | Что ждали | Факт |
|---|---|---|---|
| 1 | Бэк: `subsystems = subsystems.ActiveKeys()` → `.ToDictionary(k => k, _ => true)` (`AuthController.cs:74`) | красный | **1 упал / 2 прошло** — «AuthController.Me кладёт subsystems из массивной формы» |
| 2 | Фронт: `subsystems?: string[]` → `Record<string, boolean>` (`types/index.ts:1919`) | красный | **1 упал / 2 прошло** — «Me.subsystems в types/index.ts — string[]» |
| 3 | Ядро: `IReadOnlyList<string> ActiveKeys()` → `IReadOnlyDictionary<string, bool>` (`IAppSubsystem.cs:100`) | красный | **1 упал / 2 прошло** — «ActiveKeys() возвращает массив строк» |
| 4 | Стор: массивная ветка `setAllSubsystems` возвращена к спреду `{ ...arr }` (`lib/subsystems.ts`) | красный | **6 упало** в `subsystems.test.ts`, включая «на включённой notes в реальном JSON-ответе useSubsystem("notes")=true» |

Все четыре мутации откачены, `git status` чист (кроме untracked-теста М2),
сборка после отката зелёная.

Вывод: **сторож настоящий, и закрыт не только текст контракта, но и
поведение.** Мутации 1-3 ловят рассинхрон формы по тексту (сканер `.cs`/`.ts`),
мутация 4 показывает, что нормализация в сторе закрыта отдельным рантайм-тестом
на реальном JSON-ответе `/api/auth/me`. Именно дефект мутации 4 — тот самый
`{ ...arr }` → `{'0':'notes'}` — и дошёл в волне 3 до стенда; теперь он красит
шесть кейсов.

Известное ограничение (не дефект, но знать надо): мутации 1-3 проверяют
регэкспом **текст** C#-файлов. Переименование файла или перенос поля
`subsystems` в отдельный DTO-тип сделает сканер зелёным на пустом месте —
сообщение ассерта «ожидалось присваивание `subsystems = ...`» тогда не
сработает, потому что `expect(match).not.toBeNull()` упадёт (fail-closed), —
проверено: это поведение корректное. А вот смена ИМЕНИ метода
`ActiveKeys` на равноценный массивный геттер даст красный по неверной
причине.

## Атомарность и авторство коммитов

По коммиту на задачу — **выполнено**. Семнадцать коммитов пилота, каждый —
одно логическое изменение. «Сборных» нет.

Крупные по числу файлов два, и оба атомарны по смыслу:
`d8c73a9e` (39 файлов, +61/−46 кода и 27 тест-файлов правки вызовов) и
`a4b7b992` (40 файлов) — это одно изменение «сделать зависимость
опциональной», размазанное по всем точкам инъекции; дробить его значило бы
оставлять дерево несобираемым между коммитами.

Единственная мелкая примесь — `c83d0647` «вкрапить гейт подсистем в 15
компонентов **и тест notes**»: `notes.test.ts` к теме гейта прямого отношения
не имеет (замечание н2 прошлого прохода, не исправлено, на severity не тянет).

Авторство и трейлеры — замечания м3 и м4.

---

## Цена пилота

Сколько стоила отключаемость ОДНОЙ вертикали. Считано по 17 коммитам пилота
(мерж-коммиты master и привезённая ими чужая работа исключены); файлы —
уникальные по всем коммитам, поэтому файл, правленный дважды, посчитан один раз.

| Слой | Файлов | `+` | `−` | Всего строк |
|---|---:|---:|---:|---:|
| Backend, код | 27 | 479 | 167 | **646** |
| Backend, тесты | 30 | 274 | 135 | **409** |
| Frontend, код | 24 | 1040 | 124 | **1164** |
| Frontend, тесты | 3 | 500 | 13 | **513** |
| Документация | 3 | 1016 | 97 | **1113** |
| **Итого** | **87** | **3309** | **536** | **3845** |
| *из этого — тесты (обе стороны)* | *33* | *774* | *148* | ***922*** |
| *из этого — код без тестов и доков* | *51* | *1519* | *291* | ***1810*** |

Если смотреть только то, что уедет в продукт (код без доков): **51 файл,
1810 строк**, из них фронт 1164 против бэка 646 — фронт дороже вдвое.

### Тесты: чем именно заплатили

Позиционные вызовы, сломанные реордером параметров конструкторов, — две
волны:

| Коммит | Тест-файлов | Точек правки (хунков) | Строк |
|---|---:|---:|---:|
| `d8c73a9e` (волна 1, `NotesService`) | 27 | **35** | +68 / −68 |
| `a4b7b992` (волна 2, остальные три типа) | 29 | **39** | +205 / −66 |
| `b46b7624` (хвост после мержа master) | 1 | 1 | +1 / −1 |
| Уникальных тест-файлов по бэку | **30** | **75** | — |

Заявленные в постановке «33 позиционных вызова» — это волна 1; по хункам её
цена 35, и это только половина. Полная цена реордера — **75 точек правки в 30
тест-файлах**, причём в волне 1 все 68 изменённых строк были механическим
перечислением аргументов, без единого нового ассерта.

Важная поправка к чтению цифр: из +205 строк волны 2 механического реордера
только 68; остальные 137 — это 41 строка комментариев и 96 строк **новых
тестов**, то есть полезная работа, а не налог.

### Где осел фронтовый перерасход

| Файл | Строк | Что это |
|---|---:|---|
| `pages/SubsystemsPage.tsx` | 343 | страница подсистем в админке |
| `lib/subsystems.test.ts` | 296 | рантайм-тесты стора (165 + 131 после `b5212bb0`) |
| `eslint-rules/no-cross-feature-import.js` | 124 | правило против кросс-фичевых импортов |
| `lib/__tests__/subsystems.contract.test.ts` | 115 | сторож контракта фронт/бэк |
| `features/notes/index.ts` | 89 | фасад вертикали на фронте |
| `lib/subsystems.ts` | 96 | стор включённости (65 + 31) |
| `api/subsystems.ts` | 53 | REST-клиент |

Плюс `c83d0647`: 15 компонентов, +172/−56 — собственно вкрапление гейта
в существующий UI.

### Что эта таблица говорит для кольца 2

1. **Разовая инфраструктура уже куплена.** `SubsystemGate`,
   `SubsystemStateStore`, `/api/admin/subsystems`, стор фронта,
   `SubsystemsPage`, правило eslint, сторож контракта — это ~900 строк,
   которые вторая вертикаль не платит вовсе. Из 3845 на одноразовое ушло
   примерно четверть.
2. **Повторяемая часть — реордер конструкторов, и она линейна по числу
   потребителей.** 23 точки инъекции на спине → 75 точек правки в тестах.
   Коэффициент примерно 3 правки теста на одну точку инъекции. Для вертикали
   с 8 потребителями ждать ~25 правок тестов, для вертикали с 40 — ~120.
   Это и есть цифра, по которой считать следующую.
3. **Фронтовая часть дороже бэковой ровно там, где у вертикали есть свой
   раздел UI.** 1164 строки фронта против 646 бэка — у вертикали без
   собственного экрана (Diagnostics, WebSearch, Changelog) этой половины
   не будет.
4. **Доки стоят столько же, сколько бэк.** 1113 строк на макет, QA-отчёт и
   два ревью — сопоставимо с 1810 строками продуктового кода. Для второй
   вертикали сократится: ADR и макет уже есть.
5. **Цена, которую таблица не показывает.** Блокер Б1 — вынос контроллера в
   отдельную сборку — повторится у каждой вертикали со своим контроллером, и
   стоимость его решения в 3845 строк не входит: он ещё не решён.

---

## Что делать дальше

Порядок важен: М2 первым, иначе доказательство Б1 может исчезнуть.

1. **М2** (`7bc928e7`) — закоммитить `NotesDisabledTests.cs` красным.
2. **Б1** (`4defaacc`) — починить изоляцию REST-слоя, довести все 13 кейсов
   до зелёного, снять мёртвый код и врущие комментарии.
3. **М1** (`3bb00d85`) — обработчик на границе `BriefingController`.
4. **м1** (`a096558c`) — поднять гейт выше платного вызова модели.
5. **м3/м4** — трейлеры и авторство; на мерж не влияют, но история ветки
   половину связи с сессией потеряла, и чинить это потом будет нечем.

До закрытия Б1 решение по остальным вертикалям кольца 2 принимать рано:
пилот пока не доказал, что вертикаль со своим контроллером вообще
отключается.

---

# Подтверждение хвоста (`b5212bb0..120569a5`)

Дата: 2026-09-10. Ревьюер: Глеб. Задача `33eaaa83`.
Предмет — только девять коммитов, легших после финального ревью выше: четыре
исправления находок, два фронтовых коммита, документирование выводов и два
мержа `master`. Полного прохода нет — проверялось, закрыты ли находки, не
потерялись ли гейты при сведении и верны ли утверждения новой главы ADR.

## Вердикт одной строкой

**Мержить можно. Блокеров нет.** Б1 закрыт по существу (не «код стал
зелёным», а маршрут действительно вышел из MVC — подтверждено и механизмом в
коде, и прогоном на стенде), М1 и м1 закрыты, гейт при обоих мержах остался
единственным. Остаток — три мелочи и одна формулировка теста, которую стоит
поправить до того, как её прочтёт кольцо 2.

## Свидетельства прогона

```
dotnet test --filter "NotesDisabledTests|NotesSubsystemGateTests|SessionSummaryServiceTests"
Пройден!  : не пройдено 0, пройдено 37, пропущено 0, всего 37, 17 s

npx vitest run src/lib/__tests__/subsystems.contract.test.ts
Test Files 1 passed (1) · Tests 7 passed (7)
```

## Что проверено и чем

### Б1 — `ApplicationPart` (`0b8c0b9b`): закрыт, механизм верен

Механизм подтверждён не пересказом коммита, а сгенерированным файлом:
`backend/ClaudeHomeServer/obj/**/ClaudeHomeServer.MvcApplicationPartsAssemblyInfo.cs`
содержит 20 строк `[assembly: ApplicationPartAttribute("ClaudeHomeServer.<X>")]`,
среди них `ClaudeHomeServer.Notes`. То есть часть подключает MSBuild, наш код
на это не влияет — снимать её можно только удалением.

Новое место работает и стоит правильно: `ConfigureApplicationPartManager`
навешан на `AddControllers()` в `Program.cs:139-147`, то есть отрабатывает при
построении менеджера, до первого резолва `MvcOptions`. Сравнение по
`part.Name` корректно — у `AssemblyPart` это `Assembly.GetName().Name`, а
оригинальный объект части наружу не отдаётся.

Мёртвого кода не осталось: `using`-и `Mvc.ApplicationParts`/`Options` из
`NotesSubsystem.cs` убраны, класс `ConfigureMvcOptions` удалён, врущие
комментарии в `NotesSubsystem.cs` и `NotesController.cs` переписаны на
фактическое поведение.

Одно наблюдение без последствий: ключ и имя сборки в `Program.cs` заданы
литералами (`"notes"`, `"ClaudeHomeServer.Notes"`), а не `NotesSubsystem.Key`
и `typeof(NotesSubsystem).Assembly.GetName().Name`. Обратной ссылки Main →
вертикаль здесь избежать нельзя, а от опечатки страхует
`NotesDisabledTests` — переименование сборки или ключа уронит его. Оставляю
как есть.

### М1 — бриф (`7383cd8f`): закрыт; приём однороден, контракт наружу — нет

Дефект закрыт по-настоящему: `BriefingUnavailableException` вместо
`InvalidOperationException`, ловля на границе контроллера, 503 с причиной.
Тест хоста не вакуумный — сток лога проверяется пробой перед замером, и
автор прогнал мутацию.

Приём (свой тип исключения, ловля на границе) действительно тот же, что у
`SummaryGenerationException`. А вот наблюдаемый контракт разошёлся — см. м5.

### м1 — оплата до гейта (`1def4e92`): закрыт

Проверка `notes is null` стоит первой в `try`, до `cheap.RunAsync`;
`_inFlight` снимается в `finally`. Тест считает вызовы через
`ICheapTextRunner` и утверждает `Calls == 0` — то есть проверяет ровно то,
ради чего правка (не заплатили), а не «пришло исключение».

### Гейты при мержах (`7a82a363`, `120569a5`): не потерялись

`SubsystemGate.IsEnabled` — по-прежнему единственная точка чтения ключа:
греп по `Subsystems:` во всём `backend/**/*.cs` даёт три места —
`IAppSubsystem.cs:135` (сам гейт) и два теста, задающих настройку. Инлайновых
читателей конфига нет.

Мерж `120569a5` трогал `ChatItemView.tsx` — один из файлов, куда `a10137fd`
ставил гейт вкрапления. Гейт на месте (`useSubsystem('notes')` в строке 433,
условие рендера в 475). Остальные три вкрапления тоже целы:
`notes.ts:93`, `sync.ts:400`, `AiLauncher.tsx:131`. Опциональных точек
инъекции Notes на спине — 29 объявлений, обязательных вне вертикали нет.

### Сторож контракта (`14d0102d`): ловит, но по-прежнему вручную

Прямой ответ на вопрос постановки: **да, второй эндпоинт теперь покрыт с
обеих сторон, и покрыт настоящими проверками** — форма аргумента `Ok(...)`,
объявленный тип действия, тип-аргумент `request<…>` и форма параметра
`.then(...)` на экране. Тот дефект, что прошёл дважды, сторож поймал бы в
обоих случаях.

**Но класс дефекта он не закрывает — закрывает два его экземпляра.** Это
текстовый парсер конкретных файлов: пути захардкожены, формы вытаскиваются
регэкспами по одной ожидаемой записи вызова. Третий эндпоинт = третий блок,
написанный руками. Хуже того, он смотрит на ИСХОДНИК, а не на то, что реально
идёт по проводу: переименование поля DTO, `[JsonPropertyName]`, смена
JSON-политики именования — мимо него полностью. Fail-closed при непопадании
регэкспа есть (красный по «не нашёл»), и это правильно, но диагноз будет
уводить в сторону.

Претензии к ветке тут нет: ADR-014 вывод 5 называет это ограничение своими
словами и прямо пишет, что радикальное лечение (генерация TS-типов из
контроллеров либо контрактные тесты против поднятого бэка) **не сделано**.
Это честная запись, и место для лечения — кольцо 2, а не эта ветка.

### Выводы пилота в ADR-014 (`672d8571`): сверено с кодом

Вывод 1 — верен во всех четырёх утверждениях: генерация атрибута MSBuild
(проверено по сгенерированному файлу), `PopulateDefaultParts` независимо от
гейта, лечение удалением в композиции, сравнение по имени сборки. Оговорка
«касается всех четырнадцати вынесенных вертикалей» подтверждается: все
`ClaudeHomeServer.*.csproj` вертикалей собраны под `Microsoft.NET.Sdk.Web`.

Вывод 6 — верен и по факту, и по причине: `MapFallbackToFile("index.html")`
(`Program.cs:1618`) стоит внутри ветки «фронт найден», которой в тестовом
хосте нет.

Остальные четыре вывода сверены выборочно и расхождений с кодом не дали.

## Находки хвоста

Ни одна не блокирует вливание.

### Minor

**м5. Одна причина — два разных кода отказа.**
`SessionSummaryService.cs:55` бросает `SummaryGenerationException("Подсистема
Notes отключена…")`, а `SessionSummaryController.cs:30` отдаёт за него **502**.
`BriefingController` за ту же причину отдаёт **503 + `reason: "notes_disabled"`**.
Приём одинаков, наблюдаемый контракт — нет: 502 говорит клиенту «апстрим
сбойнул, повтори», 503 с причиной — «на этом инстансе выключено, повторять
бессмысленно». Клиенту нечем их различить, а различие существенное.
Лечение — свой тип и для «итога сессии» (или общий
`SubsystemDisabledException` на два контроллера).

**м6. Мёртвая ветка `setAllSubsystems` и комментарий, ссылающийся на удалённый код.**
`frontend/src/lib/subsystems.ts:44-45` объясняет ветку `Record<string, boolean>`
нуждами `SubsystemsPage.applyToggle`. Этого метода больше нет: `a7d08f9f`
перевёл экран в режим только чтение и удалил `Toggle`, `ConfirmDialog` и PUT.
Единственный продовый вызов — `App.tsx:457`, всегда массивом; ветка `Record`
живёт только в собственных тестах. Это ровно вывод 4 самого пилота («точку
расширения заводить, когда придёт второй участник») — применённый к пилоту.
Минимум — переписать комментарий на «оставлено под будущий PUT»; честнее —
снять ветку вместе с её тестами.

### Informational

**и1. Мотивировка фронтовых гейтов устарела на один коммит.**
`notes.ts:89`, `sync.ts:396`, `AiLauncher.tsx:127` объясняют гейт тем, что
запрос «вернул бы 500». После `0b8c0b9b` это уже не так: в тесте — 404, на
стенде — 200 `text/html`. Гейты от этого не становятся лишними, наоборот:
именно 200 `text/html` — худший из трёх исходов для fetch-клиента
(`response.ok === true`, `JSON.parse` падает синтаксической ошибкой). Стоит
поправить формулировку — она сейчас продаёт правильное решение неверным
аргументом.

**и2. м3/м4 в хвосте повторились.** `CCS-Session` есть у трёх коммитов из
семи неслияных (`a10137fd`, `a7d08f9f`, `672d8571`), нет у `0b8c0b9b`,
`1def4e92`, `7383cd8f`, `14d0102d`. Трейлер `a7d08f9f` —
`Claude MiniMax M3 <noreply@minimax.io>`: CLAUDE.md требует без «Claude» у
модели не от Anthropic (в `7383cd8f` то же сделано верно — `MiniMax M3`).
На мерж не влияет, переписать историю после вливания будет нечем.

## Вердикт по расхождению «тест зелёный / стенд 200 `text/html`»

**Цель пилота достигнута. Это не блокер вливания.** Три довода, по
убыванию веса.

**1. Стенд подтверждает изоляцию, а не опровергает её.** `200 text/html`
означает, что MVC маршрут НЕ сопоставил и запрос ушёл в общий фолбэк. Если бы
изоляция не сработала, ответ был бы 500 с записью `UnhandledExceptionHandler`
в логе — ровно то, что видели до `0b8c0b9b`. QA это и зафиксировал: «500-ки
нет, маршрут из MVC исчез». То есть предмет пилота — «выключенная вертикаль
перестаёт быть частью приложения» — доказан именно стендом.

**2. `200 text/html` — свойство приложения, а не пилота.**
`MapFallbackToFile("index.html")` (`Program.cs:1618`) стоит без ограничения
пути; единственное исключение во всём конвейере — `/_api` строкой выше.
Значит `/api/любой-несуществующий-путь` отдаёт `index.html` и на `master`, и
до этой ветки. Пилот эту дыру **обнажил, а не создал**; вливание не ухудшает
поведение прода ни на одном маршруте. Дефект `db09722c` заведён от `master` —
это верное место, и держать из-за него ветку значило бы наказывать её за
чужую находку.

**3. Что пилот не вправе утверждать.** Формулировка результата «маршруты
отдают 404» — неверна. Верная: «маршруты выходят из MVC; что клиент увидит
вместо них, решает общий фолбэк приложения». ADR-014 вывод 6 это уже
фиксирует — здесь расхождения между документом и фактом нет.

### Что делать с тестом

Тест не ложно-зелёный: верните `ApplicationPart` — и он покраснеет на 500.
Проблема в другом: **его утверждение истинно благодаря артефакту окружения**
(в тестовом хосте нет собранного фронта), а читается как утверждение о
поведении продукта. Это тот же класс, что и «сторож, который выглядит
рабочим»: сегодня он ловит нужное, но по причине, которой в проде нет.

1. **До вливания, дёшево и обязательно.** Поправить шапку `NotesDisabledTests`
   и текст ассерта: предмет проверки — «MVC не сопоставил action», а не
   «клиент получает 404»; одной фразой сказать, что на проде вместо 404
   приезжает SPA-фолбэк, со ссылкой на `db09722c`. Без этого следующий
   читатель поверит тексту теста, а не стенду, — а вывод 6 живёт в другом
   файле и до него не дойдёт.
2. **Правильно, но не в этой ветке.** Перевести утверждение на инвариант, не
   зависящий от окружения: ни одного эндпоинта `NotesController` в
   `EndpointDataSource` хоста при закрытом гейте (контроль — при открытом
   есть). Это ровно то, что обязан обеспечить гейт, и это не перестанет
   что-либо проверять после починки `db09722c` — тогда как нынешний ассерт
   после починки станет верным, но проверять будет фолбэк, а не гейт.
   HTTP-кейсы оставить, сменив их предмет на «не 500 и нет записи
   `UnhandledExceptionHandler`».

## Итог

Мержить. Ветка делает то, ради чего затевалась: вертикаль со своим
контроллером отключается и при этом действительно изолируется — что и было
единственным нерешённым вопросом финального ревью. Решение по кольцу 2
принимать можно, с поправкой на цену вывода 1: у каждой следующей вертикали
со своим контроллером это отдельная строка в `Program.cs`, а не бесплатное
следствие гейта.
