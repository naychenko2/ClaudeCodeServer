// Публичная точка входа реестра фронтовых подсистем.
//
// Реэкспортирует механизм из registryCore и собирает РЕГИСТРАЦИЮ подсистем:
// одна строка side-effect-импорта на подсистему. Удалить подсистему = удалить
// её папку и эту строку — каркас соберётся, слоты и раздел хаба опустеют.
//
// Side-effect (а не явный вызов registerSubsystem(manifest)) выбран намеренно:
// так регистрация — ровно одна строка, и её удаление не оставляет висящего
// неиспользуемого импорта (в проекте включён noUnusedLocals). Модуль фичи сам
// зовёт registerSubsystem из registryCore при импорте.
export * from './registryCore';

// ===== Регистрация подсистем: одна строка — одна подсистема =====
// eslint-disable-next-line module/no-cross-feature-import -- это и есть та самая единственная строка, которой каркас знает о фиче: side-effect-импорт её регистратора (manifest + вклады слотов)
import '../../features/notes/registry';
