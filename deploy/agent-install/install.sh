#!/bin/sh
# Установщик агента AI Home для локальных проектов (Linux, POSIX sh).
# ЗАГЛУШКА (agent-distribution AD-3): сервер уже раздаёт этот файл по /agent/install.sh,
# само содержимое пишет задача AD-5s.
echo "Установщик агента ещё не готов: этот сервер раздаёт заглушку." >&2
exit 1
